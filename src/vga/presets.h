/*
    Presets for printing out the TUI for TAIL bootloader.
*/

#pragma once

#ifndef _TAIL_PRESETS_
#define _TAIL_PRESETS_

// A matrix that contains each row for information screen.
char INFO_SCREEN_DATA[][2] = {
    {'a'}, {'t', 'e', 's', 't'}
};
 
#endif